---
title: "Project"
author: "Jeff Lancaster"
date: "November 5, 2017"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
holidays_events<-read_csv("Stat 231 Final Project/holidays_events.csv")
items<-read_csv("Stat 231 Final Project/items.csv")
oil<-read_csv("Stat 231 Final Project/oil.csv")
sample_submission<-read_csv("Stat 231 Final Project/sample_submission.csv")
stores<-read_csv("Stat 231 Final Project/stores.csv")
test<-read_csv("Stat 231 Final Project/test.csv")
train<-read_csv("Stat 231 Final Project/train.csv")
transactions<-read_csv("Stat 231 Final Project/transactions.csv")
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
